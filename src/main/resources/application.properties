# Server
server.port=8080

# OpenAI — set OPENAI_API_KEY in your environment or .env file
openai.api.key=${OPENAI_API_KEY}

# Optional: reduce Spring Boot startup noise
spring.main.banner-mode=off
logging.level.dev.langchain4j=DEBUG
